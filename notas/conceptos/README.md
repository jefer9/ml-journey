# Conceptos

Una nota por idea. Atómica: si la nota explica dos cosas, son dos notas.

**La regla:** si copiaste y pegaste, no cuenta. Se escribe con tus palabras o
no se escribe. Una nota que no podés explicar en voz alta está incompleta.

Se enlazan entre sí con `[[wikilinks]]`. Con el tiempo el grafo de Obsidian
muestra qué áreas están conectadas y cuáles quedaron sueltas — eso suele
señalar exactamente lo que no entendiste.

Plantilla: [`_plantilla.md`](_plantilla.md)

---

## Backlog por fase

Se escriben cuando el tema aparece, no antes.

### Fase 0
`vector` · `matriz` · `producto-punto` · `broadcasting` · `derivada` ·
`gradiente` · `distribucion` · `media-y-varianza` · `bayes` · `verosimilitud`

### Fase 1
`overfitting` · `underfitting` · `sesgo-vs-varianza` · `train-test-split` ·
`validacion-cruzada` · `data-leakage` · `feature-engineering` ·
`regresion-lineal` · `regresion-logistica` · `arbol-de-decision` ·
`random-forest` · `gradient-boosting` · `k-means` · `precision-y-recall` ·
`f1` · `auc-roc` · `matriz-de-confusion` · `clases-desbalanceadas` ·
`normalizacion` · `regularizacion`

### Fase 2
`neurona` · `funcion-de-activacion` · `funcion-de-perdida` ·
`descenso-de-gradiente` · `backpropagation` · `learning-rate` · `batch` ·
`epoca` · `dropout` · `embedding` · `cnn` · `atencion` · `transformer` ·
`tokenizacion`

### Fase 3
`rag` · `chunking` · `base-vectorial` · `busqueda-semantica` · `reranking` ·
`function-calling` · `agente` · `evals` · `lora` · `qlora` · `cuantizacion` ·
`dataset-de-instrucciones` · `cuando-no-hacer-fine-tuning`

### Fase 4
`serving` · `drift` · `versionado-de-datos` · `tracking-de-experimentos` ·
`ci-cd-de-modelos`
